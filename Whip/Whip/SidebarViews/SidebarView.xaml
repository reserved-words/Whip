<UserControl x:Class="Whip.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Whip"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <local:CurrentTrackMiniView DataContext="{Binding CurrentTrackMiniViewModel}"
                                    Grid.Row="0"/>

        <local:PlayerControlsView DataContext="{Binding PlayerControlsViewModel}"
                                  Grid.Row="1"/>

        <local:SettingsIconsView DataContext="{Binding SettingsIconsViewModel}"
                                 Grid.Row="2"/>

    </Grid>
</UserControl>
